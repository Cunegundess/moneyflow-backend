apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: moneyflow
  labels:
    app: redis
    component: infrastructure
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
