apiVersion: v1
kind: Service
metadata:
  name: auth-middleware
  namespace: moneyflow
  labels:
    app: auth-middleware
    component: microservice
spec:
  selector:
    app: auth-middleware
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
